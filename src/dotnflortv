import React from 'react';
import { Play, Info } from 'lucide-react';

const NetflixClone = () => {
  // מידע לדוגמה על סרטים
  const movies = [
    {
      title: "סרט לדוגמה 1",
      image: "/api/placeholder/300/169"
    },
    {
      title: "סרט לדוגמה 2",
      image: "/api/placeholder/300/169"
    },
    {
      title: "סרט לדוגמה 3",
      image: "/api/placeholder/300/169"
    }
  ];

  return (
    <div className="min-h-screen bg-black text-white">
      {/* תפריט ניווט */}
      <nav className="flex items-center p-4 bg-black bg-opacity-90">
        <div className="text-red-600 text-2xl font-bold">NETFLIX</div>
        <div className="ml-8 space-x-4">
          <button className="hover:text-gray-300">דף הבית</button>
          <button className="hover:text-gray-300">סדרות</button>
          <button className="hover:text-gray-300">סרטים</button>
        </div>
      </nav>

      {/* באנר ראשי */}
      <div className="relative h-96">
        <img 
          src="/api/placeholder/1200/500" 
          alt="באנר ראשי"
          className="w-full h-full object-cover"
        />
        <div className="absolute bottom-0 left-0 p-8 bg-gradient-to-t from-black">
          <h1 className="text-4xl font-bold mb-4">כותרת הסרט</h1>
          <div className="flex space-x-4">
            <button className="flex items-center px-6 py-2 bg-white text-black rounded hover:bg-opacity-80">
              <Play className="mr-2" /> נגן
            </button>
            <button className="flex items-center px-6 py-2 bg-gray-500 bg-opacity-50 rounded hover:bg-opacity-40">
              <Info className="mr-2" /> מידע נוסף
            </button>
          </div>
        </div>
      </div>

      {/* שורת סרטים */}
      <div className="p-8">
        <h2 className="text-xl font-semibold mb-4">פופולרי כעת</h2>
        <div className="flex space-x-4">
          {movies.map((movie, index) => (
            <div key={index} className="flex-none w-64">
              <img 
                src={movie.image} 
                alt={movie.title}
                className="w-full rounded hover:scale-105 transition-transform duration-300"
              />
              <p className="mt-2">{movie.title}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};

export default NetflixClone;
